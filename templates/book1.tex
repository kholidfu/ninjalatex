\documentclass{article}

% preambule
% needed package

% set pdf title metadata
\usepackage[pdftex,
  pdfauthor={ Joan De PDF },
  pdftitle={((( title|title ))) - google.com},
  pdfsubject={((( title )))},
  pdfkeywords={((( keywords )))},
  hidelinks=true]{hyperref}

% barcode
\usepackage{pst-barcode}
\usepackage{auto-pst-pdf}

% hypersetup
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue
}

% cover coloring
\usepackage{xcolor}
%\definecolor{titlepagecolor}{cmyk}{1,.60,0,.40}
\definecolor{titlepagecolor}{cmyk}{((( colors )))}

% image
\usepackage{graphicx}

% url need this
% \usepackage[hidelinks=true]{hyperref}

% custom paper size
%\usepackage[paperheight=233mm, paperwidth=178mm, top=1in,bottom=1in,right=1in,left=1in]{geometry}
\usepackage[a4paper,right=1in]{geometry}

% custom header footer layout settings
\usepackage{fancyhdr}
    \pagestyle{fancy}
    \rhead{}
    \rfoot{}
    \lhead[RO,RE]{Download \textbf{((( title|title ))) PDF eBook}}
    \lfoot{}
    \cfoot{}
% remove horizontal line from fancyhdr
%\renewcommand{\headrulewidth}{0pt}


\usepackage{color} % <= font color


\begin{document}

((( title )))

\includegraphics{(((image)))}


\end{document}
